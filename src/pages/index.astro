---
import ArticleBody from "@/components/ArticleBody.astro";
import ArticleHero from "@/components/ArticleHero.astro";
import SiteFooter from "@/components/SiteFooter.astro";
import TopBar from "@/components/TopBar.astro";
import { sections } from "@/data/article";
import { profile } from "@/data/profile";
import BaseLayout from "@/layouts/BaseLayout.astro";

const title = "RAMEN Case Study";
const description =
  "How I planned, designed, built, and validated RAMEN, a browser-capable agent system with a Chrome-extension companion.";
---

<BaseLayout title={title} description={description}>
  <TopBar activePage="caseStudy" />

  <main id="main-content" class="article-shell">
    <ArticleHero profile={profile} />
    <ArticleBody sections={sections} />
  </main>

  <SiteFooter />
</BaseLayout>
